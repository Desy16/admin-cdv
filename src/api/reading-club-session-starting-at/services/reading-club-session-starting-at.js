'use strict';

/**
 * reading-club-session-starting-at service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::reading-club-session-starting-at.reading-club-session-starting-at');
